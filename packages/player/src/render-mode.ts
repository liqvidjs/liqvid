/**
 * - `screenshot`: rendering to take a screenshot of one frame
 * - `thumbs`: rendering to generate thumbnails
 * - `video`: static video export
 * - `web`: the default experience
 * */
export type RenderMode = "screenshot" | "thumbs" | "video" | "web";
