@media (min-width: 401px)    
  .rp-controls
    --rp-controls-left 2
    --rp-controls-right 3

@media (max-width: 800px)
  .ractive-player
    --rp-controls-height 36px

@media (max-width: 400px)
  .ractive-player
    --rp-controls-height 30px
    
  .rp-controls
    --rp-controls-left 2
    --rp-controls-right 2
    
  .rp-controls-settings
    display none

@media (any-hover: none)
  .rp-controls
    color #FFF
    height var(--rp-controls-height)
    position absolute
    top unset
    transition opacity .25s cubic-bezier(0.0,0.0,0.2,1)
    width var(--rp-width)
    bottom calc((100% - var(--rp-height)) / 2)
    user-select none
    z-index 1000
    left calc((100% - var(--rp-width)) / 2)
    
  // hide certain controls
  .rp-controls-volume
    display none !important
  
  // time
  .rp-controls-time
    line-height unset

  .rp-current-time, .rp-total-time
    height var(--rp-buttons-height)
    line-height var(--rp-buttons-height)
    position absolute
    top 0
    width var(--rp-buttons-height)
    
  .rp-current-time
    left 0
    text-align right
    
  .rp-total-time
    right calc((var(--rp-controls-right) - 1) * var(--rp-buttons-height))
    text-align left

  .rp-time-separator
    display none
    
  .rp-controls-scrub-wrap
    position relative
    
    &::after
      content ""
      position absolute
      top calc(var(--rp-buttons-height) * (0.075 / 0.55 - 0.5))
      bottom calc(var(--rp-buttons-height) * (0.075 / 0.55 - 0.5))
      left 0
      right 0
    
  .rp-controls-scrub
    height calc(7.5% / 0.55)
    top unset
    top calc(50% - 0.5 * (7.5% / 0.55))
    width calc(100% - (var(--rp-controls-left) + var(--rp-controls-right)) * var(--rp-buttons-height) - 7px)
    left calc(var(--rp-controls-left) * var(--rp-buttons-height))
    margin unset
    
  .rp-scrubber
    height 216%
    top -50%
    pointer-events all
    
  .rp-controls-playpause
    cursor pointer
    pointer-events all

    position absolute
    left var(--rp-buttons-height)
    top 0
    width var(--rp-buttons-height)

  .rp-controls-right
    float unset
    position absolute
    right 0
    top 0
    
    > *
      width var(--rp-buttons-height)

